1. 词法分析
	词法分析是编译器处理流程中的第一步，它顺序扫描源文件的字符，
	通过与词法记号的有限自动机进行匹配，产生各式各样的词法记号。

	扫描器：将从源文件内按序扫描字符的功能独立出来
	解析器：与有限自动机进行匹配产生词法记号的功能

	1.1 扫描器
		读取源文件，按序返回文件内的字符，直到文件结束。

		预读机制：
			使用一块缓冲区保存后续的多个字符，每次调用scan时，首先从缓冲区内
			按序获取字符，只有缓冲区为空时，才从磁盘重新加载缓冲区。

			代码见my_scan.cpp

	1.2 